library(quanteda)
library(readtext)

# Load the text into R, use readtext(). Remember, we have c("no", "type", "party") separated by dvsep = "_" !!

# Create corpus, put something meaningful into docnames(corpus), for example text$doc_id
# Create dfm


########################### Task 1 #####################################
# Read 5 articles (one from each party), try to assign a score to them (for example 1 for the "leftest", 10 for the "rightest")
# Run a Wordscore
# Plot it


########################### Task 2 #####################################
# Read 5 more articles (one from each party, 10 articles in total), try to reassign the scores (for example 1 for the "leftest", 10 for the "rightest")
# Run a Wordscore
# Plot it


########################### Task 3 #####################################
# Read 5 more articles again (one from each party, 15 articles in total), try to reassign the scores (for example 1 for the "leftest", 10 for the "rightest")
# Run a Wordscore
# Plot it

